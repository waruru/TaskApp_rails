<h1>Boards#show</h1>
<p>ボード名：<%= @board.name %></p>

<% if @task_lists.exists? %>
  <ul>
    <% @task_lists.each do |task_list| %>
      <li><%= task_list.name %>
        <% if task_list.tasks.exists? %>
          <ul>
            <% task_list.tasks.each do |task| %>
              <li><%= task.title %></li>
            <% end %>
          </ul>
        <% end %>
      </li>
    <% end %>
  </ul>
<% end %>
<p><%= link_to "リストを追加", new_board_task_list_path(@board) %></p>

<%= link_to "削除", @board, method: :delete %>
